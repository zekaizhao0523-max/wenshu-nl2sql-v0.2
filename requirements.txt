# 问数 Agent 依赖（元数据同步 + 向量索引 + 管理平台）

# L1 元数据库
sqlalchemy>=2.0
pymysql>=1.1
python-dotenv>=1.0

# Web 管理平台
fastapi>=0.111
uvicorn[standard]>=0.29
typing_extensions>=4.12

# 可选驱动（按所选引擎安装）
# psycopg2-binary>=2.9
# pyhive>=0.7
# clickhouse-connect>=0.7
# oracledb>=2.0
# pymssql>=2.2
# trino>=0.328
# impyla>=0.19
# pymilvus>=2.4
# chromadb>=0.5

# 向量库（默认 Qdrant）
qdrant-client>=1.9

# 本地免费 embedding（推荐 Qwen3-Embedding-0.6B）
sentence-transformers>=2.7
transformers>=4.51.0

# PyTorch — GPU 版请运行 scripts/install_gpu_torch.ps1（NJU 镜像 cu118）
# CPU 版: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.4.0

# 可选：云端 embedding
# openai>=1.30
